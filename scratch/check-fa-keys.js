const fa = require('react-icons/fa');
console.log('Fa Java keys:', Object.keys(fa).filter(k => k.toLowerCase().includes('java')));
console.log('Fa Oracle keys:', Object.keys(fa).filter(k => k.toLowerCase().includes('oracle')));
