const si = require('react-icons/si');
console.log('Oracle keys:', Object.keys(si).filter(k => k.toLowerCase().includes('oracle')));
console.log('Java keys:', Object.keys(si).filter(k => k.toLowerCase().includes('java')));
console.log('HackerRank keys:', Object.keys(si).filter(k => k.toLowerCase().includes('hackerrank')));
