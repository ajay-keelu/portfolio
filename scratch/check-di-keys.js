const di = require('react-icons/di');
console.log('Di Oracle keys:', Object.keys(di).filter(k => k.toLowerCase().includes('oracle')));
console.log('Di Java keys:', Object.keys(di).filter(k => k.toLowerCase().includes('java')));
